const spawn = require('child_process').spawn

const s = spawn('node', ['out'], {detached: true})

s.stdout.on('data', (data) => {
  console.log(data.toString('utf-8'))
})

module.exports = (name) => { return `hey ${name}, i'm fen` }
